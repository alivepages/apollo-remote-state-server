overwrite: true
schema: "http://localhost:" + (process.env.PORT || 400) 
generates:
  ./src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
